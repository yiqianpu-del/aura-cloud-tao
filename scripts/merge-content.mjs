/**
 * Content merge script — now a no-op.
 * Content is managed directly in src/data/siteContent.json.
 * This script exists only because the project build command references it.
 */
console.log('Content data is ready (using local siteContent.json)');
